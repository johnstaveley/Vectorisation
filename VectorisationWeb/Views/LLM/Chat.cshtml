@model EmbeddingService.Models.LLMRequest
@{
    ViewData["Title"] = "Chat with LLM";
}

<h1>Chat with LLM</h1>
<p class="text-muted">Have a conversation with the AI assistant</p>

<nav aria-label="breadcrumb">
    <ol class="breadcrumb">
        <li class="breadcrumb-item"><a asp-action="Index">LLM Services</a></li>
        <li class="breadcrumb-item active" aria-current="page">Chat</li>
    </ol>
</nav>

@if (TempData["Error"] != null)
{
    <div class="alert alert-danger alert-dismissible fade show" role="alert">
        @TempData["Error"]
        <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
    </div>
}

<div class="row">
    <div class="col-md-8">
        <div class="card">
            <div class="card-body">
                <form asp-action="Chat" method="post">
                    <div class="mb-3">
                        <label asp-for="Prompt" class="form-label">Your Message</label>
                        <textarea asp-for="Prompt" class="form-control" rows="4" 
                                  placeholder="Type your message here... e.g., 'What is the capital of France?'"></textarea>
                        <span asp-validation-for="Prompt" class="text-danger"></span>
                    </div>
                    
                    <div class="accordion mb-3" id="optionsAccordion">
                        <div class="accordion-item">
                            <h2 class="accordion-header">
                                <button class="accordion-button collapsed" type="button" 
                                        data-bs-toggle="collapse" data-bs-target="#collapseOptions">
                                    Advanced Options
                                </button>
                            </h2>
                            <div id="collapseOptions" class="accordion-collapse collapse" 
                                 data-bs-parent="#optionsAccordion">
                                <div class="accordion-body">
                                    <div class="row">
                                        <div class="col-md-4">
                                            <div class="mb-3">
                                                <label asp-for="Options.Temperature" class="form-label">
                                                    Temperature
                                                    <small class="text-muted">(0.0 - 2.0)</small>
                                                </label>
                                                <input asp-for="Options.Temperature" type="number" 
                                                       class="form-control" step="0.1" min="0" max="2" value="0.7" />
                                            </div>
                                        </div>
                                        <div class="col-md-4">
                                            <div class="mb-3">
                                                <label asp-for="Options.TopP" class="form-label">
                                                    Top P
                                                    <small class="text-muted">(0.0 - 1.0)</small>
                                                </label>
                                                <input asp-for="Options.TopP" type="number" 
                                                       class="form-control" step="0.05" min="0" max="1" value="0.9" />
                                            </div>
                                        </div>
                                        <div class="col-md-4">
                                            <div class="mb-3">
                                                <label asp-for="Options.MaxTokens" class="form-label">
                                                    Max Tokens
                                                </label>
                                                <input asp-for="Options.MaxTokens" type="number" 
                                                       class="form-control" min="1" max="4096" value="2000" />
                                            </div>
                                        </div>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>
                    
                    <button type="submit" class="btn btn-success">
                        <span class="spinner-border spinner-border-sm d-none" role="status"></span>
                        Send Message
                    </button>
                    <a asp-action="Index" class="btn btn-secondary">Cancel</a>
                </form>
            </div>
        </div>
    </div>
    
    <div class="col-md-4">
        <div class="card bg-light">
            <div class="card-body">
                <h6 class="card-title">Chat Tips</h6>
                <ul class="small mb-0">
                    <li>Ask clear, specific questions</li>
                    <li>You can ask follow-up questions</li>
                    <li>The AI can help with coding, writing, analysis, and more</li>
                    <li>Be patient - complex queries take longer</li>
                </ul>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
    <script>
        document.querySelector('form').addEventListener('submit', function() {
            var btn = document.querySelector('button[type="submit"]');
            btn.querySelector('.spinner-border').classList.remove('d-none');
            btn.disabled = true;
        });
    </script>
}
